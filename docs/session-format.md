# Session Format

A successful session should contain:

- metadata.json
- raw optical CSV
- events TSV
- optional IMU CSV
- validated SNIRF only when all required scientific metadata are available

Raw data must remain recoverable even when standards-compliant SNIRF cannot be produced.
