# v1.3 Setup

Workflow:

1. Connect Mendi
2. Start notification capture
3. Record baseline session
4. Export raw packets
5. Analyze packet patterns

Git:

git add .
git commit -m "ANR Mendi Recorder v1.3 packet capture decoder"
git push
